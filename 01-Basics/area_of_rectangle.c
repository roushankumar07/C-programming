#include<stdio.h>

    int main(){
        int length , breadth;
        printf("What is the length of rectange is\n");
        scanf("%d",&length);

        printf("What is the breadth of rectangle is\n");
        scanf("%d",&breadth);

        printf("The area of rectangle is %d", length*breadth);

        return 0;

    }
